# Capability Truth Matrix

| # | Capability | Implementation File(s) | Mechanism | Boundary | Test Coverage | Status | Evidence / Note |
|---|---|---|---|---|---|---|---|
| 1 | **APK / Binary SHA-256 Hashing** | `src/utils/binaryInspector.ts`, `src/core/crypto/Sha256.ts` | W3C `crypto.subtle.digest('SHA-256')` | Browser Web Crypto | `TestSuite.ts` (PIPE-PROV-001..035) | **A. REAL + EXECUTED** | Computes 64-character lowercase hex digest over byte buffers |
| 2 | **Shannon Entropy Calculation** | `src/utils/binaryInspector.ts`, `ShannonEntropyDetector.ts` | $H = -\sum p_i \log_2 p_i$ | Client-side Byte Math | `TestSuite.ts` (DET-SIG-013..018) | **A. REAL + EXECUTED** | Range 0.0–8.0; strictly classified as heuristic indicator |
| 3 | **Magic Byte Header Dissection** | `src/utils/binaryInspector.ts` | First 4 bytes hex matching (`50 4B 03 04`, `7F 45 4C 46`, etc.) | Client-side Byte Array | Manual & Unit inspection | **A. REAL + EXECUTED** | Identifies APK/ZIP, ELF, DEX, PDF files |
| 4 | **Punycode / Homograph Detection** | `src/core/detection/detectors/PunycodeDetector.ts` | Regex `/^xn--/i` on normalized DNS labels | Deterministic Core | `TestSuite.ts` (DET-SIG-001..006) | **A. REAL + EXECUTED** | 100% confidence on RFC 3492 encoded labels |
| 5 | **RFC 3986 Identity Normalizer** | `src/core/identity/Normalizer.ts` | WHATWG URL + Public Suffix Parsing | Deterministic Core | `TestSuite.ts` (ID-NORM-001..030) | **A. REAL + EXECUTED** | Lowercases, strips credentials, default ports, and fragments |
| 6 | **Integer Risk Engine** | `src/core/detection/RiskEngine.ts` | Safe integer math, family caps (`DOMAIN:50`, `CONTENT:40`), overflow guards | Deterministic Core | `TestSuite.ts` (RISK-ARITH-001..030) | **A. REAL + EXECUTED** | Strictly enforces integers, rejects floats / NaN |
| 7 | **STIX 2.1 & JSON-LD Export** | `src/core/export/ForensicExporter.ts` | OASIS STIX 2.1 & W3C JSON-LD JSON generation | Client/Server JSON | `ForensicCoreView.tsx` | **A. REAL + EXECUTED** | Produces valid STIX `bundle` and JSON-LD provenance graphs |
| 8 | **Microphone MediaStream Interception** | `src/utils/mediaStreamRig.ts` | `navigator.mediaDevices.getUserMedia()`, Web Audio `AnalyserNode` | Browser Sandbox | Visual test via `PrivacyShieldView` | **A. REAL + EXECUTED** | Controls active `MediaStreamTrack` and zeros buffers; bounded as browser-level |
| 9 | **Hardware & Browser Prober** | `src/utils/systemProber.ts` | `navigator.hardwareConcurrency`, `deviceMemory`, `storage.estimate()` | W3C Web APIs | `DiagnosticsView.tsx` | **A. REAL + EXECUTED** | Probes live browser environment without fabricating native kernel data |
| 10 | **Gemini AI Threat Analysis** | `server.ts` (`/api/security/analyze`) | Google GenAI SDK with heuristic fallback | Server-side API | End-to-end endpoint | **A. REAL + EXECUTED** | Protected on server; returns JSON threat classification |
| 11 | **Encrypted Vault Storage** | `src/components/SecureVaultView.tsx` | React UI state with PIN keypad & decoy mode | Client-side UI | UI interaction | **C. SIMULATION / MOCK** | UI demo of vault PIN & decoy split; does not currently persist WebCrypto AES-GCM ciphertext to disk |
| 12 | **OS Permission Revocation** | `src/components/PrivacyShieldView.tsx` | React state toggles for app permissions | Client-side UI | UI interaction | **C. SIMULATION / MOCK** | Bounded as sandbox simulation; browsers cannot alter OS app permissions |
| 13 | **WireGuard VPN Tunneling** | `server.ts` (`/api/vpn/nodes`), `NetworkDefenseView.tsx` | Synthetic network telemetry & ping probes | Web Application | UI interaction | **C. SIMULATION / MOCK** | Bounded as dashboard representation; web apps cannot create raw TUN/TAP devices |
| 14 | **ARP / Packet Sniffing** | `src/components/NetworkDefenseView.tsx` | Synthetic WiFi & gateway inspection charts | Web Application | UI interaction | **C. SIMULATION / MOCK** | Bounded as simulated defense center |
| 15 | **Remote Device Wipe / Lockdown** | `src/components/AntiTheftView.tsx`, `RemoteLockOverlay.tsx` | Browser overlay lock with PIN unlock | Web Application | UI interaction | **D. PARTIAL / SANDBOX** | Locks the active web application window; cannot lock host Android OS |
